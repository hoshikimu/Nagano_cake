<table class="genre-table">
  <thead>
    <tr><th>ジャンル検索</th></tr>
  </thead>
  <tbody>
    <% genres.each do |genre| %>
      <tr><td><%= link_to genre.name, genre_item_path(genre) %></td></tr>
    <% end %>
  </tbody>
</table>