<%# 顧客用トップページ %>

<%# 後ほど実装、Each文とリンク？ %>
<div class="container homes-top-wrapper">
  <div class="row">

    <table class="col-xs-2 homes-top-genre-search">
      <thead>
        <tr class="homes-top-genre-title">
          <td>ジャンル検索</td>
        </tr>
      </thead>
      <% @genres.each do |genre| %>
        <tbody>
          <tr>
            <td class="homes-top-genre-text">
              <%= link_to items_path do %>
                <%= genre.name %>
              <% end %>
            </td>
          </tr>
        </tbody>
       <% end %>
    </table>

    <div class="homes-top-image col-xs-8 col-xs-offset-1">
      <div class="homes-top-text col-xs-10 col-xs-offset-1">
        <p>
          ようこそ、Nagano_cakeへ！<br>
          このサイトは、ケーキ販売のECサイトになります。<br>
          会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。
        </p>
      </div>
    </div>

  </div>
</div>

    <%# 後ほど実装、Each文とリンク %>
    <%# リンク先は商品詳細ページ item_path(@item.id) %>
<div class="col-xs-offset-4 homes-top-under-menu-title">
  <h2>オススメ商品</h2>
</div>
<div class="homes-top-under-menu col-xs-8 col-xs-offset-3">
  <% @items.each do |item| %>
    <table>
      <thead>
        <tr>
          <td>
            <%= link_to items_path do %>
              <%= attachment_image_tag item, :image, :fill, 250, 150, fallback: "no_image.jpg", size:'200x140' %>
            <% end %>
          </td>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= item.name %></td>
        </tr>
        <tr>
          <td>￥<%= item.price %></td>
        </tr>
      </tbody>
    </table>
  <% end %>
</div>
<div class="col-xs-1 col-xs-offset-10">
    <p><%= link_to 'もっと見る  >', items_path %></p>
</div>
